import { CalculateEndDatePipePipe } from './calculate-end-date-pipe-pipe';

describe('CalculateEndDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateEndDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
